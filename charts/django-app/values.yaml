replicaCount: 2

image:
  repository: 666834592660.dkr.ecr.eu-central-1.amazonaws.com/lesson-9-django-app
  pullPolicy: Always
  tag: "latest"

nameOverride: ""
fullnameOverride: ""

service:
  type: LoadBalancer
  port: 80
  targetPort: 8000

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

config:
  SECRET_KEY: "django-insecure-u(qc9*j0w=ut5db3f#izfz_8szd7rruds!p8p5hgwz0in=+s7t"
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  # Database configuration - these will be overridden by Terraform outputs
  POSTGRES_DB: "djangodb"
  POSTGRES_USER: "djangouser"
  POSTGRES_PASSWORD: "changeme"  # Should be set via helm upgrade --set
  POSTGRES_HOST: "rds-endpoint.region.rds.amazonaws.com"  # Should be set via helm upgrade --set
  POSTGRES_PORT: "5432"

nodeSelector: {}
tolerations: []
affinity: {}